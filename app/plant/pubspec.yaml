name: plant_disease_detection
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  camera: ^0.10.0+1
  image_picker: ^0.8.4+3
  tflite_flutter: ^0.9.0
  tflite_flutter_helper: ^0.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/clover_labels.txt
    - assets/clover_model.h5
    - assets/plant_labels.txt
    - assets/plant_model.h5
